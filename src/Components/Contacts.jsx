import React from 'react';
import ReactDOM from 'react-dom/client';

export default function Contacts() {
    return(
        <div className="contacts-container">
            <p className="email-list">Email</p>
            <p className="linked-in-list">Linked In</p>
        </div>

    );
}